version: '3.8'
services:
  backend:
    build: ../backend
    container_name: havit-backend
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    command: npm run start:dev
